---
import '../styles/main.css';

interface Props {
  title: string;
  description?: string;
  noindex?: boolean;
  schema?: string;
}

const {
  title,
  description = 'Family-owned legal document preparation in Grants Pass (GP), Oregon since 1995.',
  noindex = false,
  schema,
} = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');
const { pathname } = Astro.url;
const canonicalURL = `https://www.a1paralegal.com${pathname}`;
const fullTitle = `${title} | A1 Quality Paralegal`;

function isActive(href: string) {
  if (href === base) return pathname === base || pathname === base.slice(0, -1);
  return pathname.startsWith(href);
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex, nofollow" />}
    <title>{fullTitle}</title>
    <link rel="canonical" href={canonicalURL} />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta name="theme-color" content="#1e3a8a" />
    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:image" content="https://www.a1paralegal.com/images/og-image.jpg" />
    <meta property="og:site_name" content="A1 Quality Paralegal" />
    <meta name="twitter:card" content="summary_large_image" />
    {schema && <script type="application/ld+json" set:html={schema} />}
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-2LJ5JF2FYY"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-2LJ5JF2FYY');
    </script>
    <!-- Microsoft Clarity -->
    <script type="text/javascript">
      (function(c,l,a,r,i,t,y){
        c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
        t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
        y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
      })(window, document, "clarity", "script", "wcutf6ptwu");
    </script>
  </head>
  <body>
    <header class="site-header">
      <div class="site-header__inner">
        <div class="site-brand">
          <a class="site-logo" href={base}>
            <img src={`${base}images/logo.png`} alt="A1 Quality Paralegal" loading="eager" height="58" />
          </a>
        </div>

        <button
          class="nav-toggle"
          aria-label="Toggle navigation"
          aria-expanded="false"
          aria-controls="primary-nav"
        >
          <span class="nav-toggle__bar"></span>
          <span class="nav-toggle__bar"></span>
          <span class="nav-toggle__bar"></span>
        </button>

        <nav class="site-nav" id="primary-nav" aria-label="Site">
          <ul class="site-nav__list">
            <li>
              <a href={base} aria-current={isActive(base) ? 'page' : undefined}>Home</a>
            </li>
            <li>
              <a
                href={`${base}estate-planning/`}
                aria-current={isActive(`${base}estate-planning/`) ? 'page' : undefined}
              >Estate Planning</a>
            </li>
            <li class="nav-has-dropdown">
              <button class="nav-dropdown-toggle" aria-expanded="false" aria-haspopup="true">
                More Services <span class="nav-dropdown-arrow">▾</span>
              </button>
              <ul class="nav-dropdown" role="menu">
                <li><a href={`${base}power-of-attorney/`}>Powers of Attorney</a></li>
                <li><a href={`${base}deeds-transfers/`}>Deeds &amp; Transfers</a></li>
                <li><a href={`${base}adoption/`}>Adoption</a></li>
                <li><a href={`${base}probate/`}>Probate &amp; Estate Settlement</a></li>
                <li><a href={`${base}llc-formation/`}>LLC Formation</a></li>
                <li><a href={`${base}real-estate-sales/`}>Real Estate Sales</a></li>
              </ul>
            </li>
            <li>
              <a
                href={`${base}contact-us/`}
                aria-current={isActive(`${base}contact-us/`) ? 'page' : undefined}
              >Contact &amp; Book</a>
            </li>
          </ul>
        </nav>

        <a class="header-phone" href="tel:+15414742260">541-474-2260</a>
      </div>
    </header>

    <main id="main" class="site-main" role="main">
      <slot />
    </main>

    <footer class="site-footer">
      <div class="site-footer__inner">
        <div class="footer-brand">
          <a class="site-logo" href={base}>
            <img src={`${base}images/logo.png`} alt="A1 Quality Paralegal" loading="lazy" height="58" />
          </a>
          <p class="footer-tagline">A1 Quality Paralegal LLC</p>
          <p class="footer-tagline">Personal, professional, and friendly service since 1995.</p>
        </div>
        <div class="footer-contact">
          <h3>Contact</h3>
          <ul class="footer-contact__list">
            <li>
              <a
                href="https://maps.google.com/?q=327+NE+6th+St+%231,+Grants+Pass,+OR+97526"
                target="_blank"
                rel="noopener noreferrer"
              >327 NE 6th St #1, Grants Pass, OR 97526</a>
            </li>
            <li><a href="tel:+15414742260">541-474-2260</a></li>
            <li class="footer-voicemail">Our office is run by a small dedicated team — if you reach voicemail, leave a message and we'll get back to you promptly.</li>
            <li><a href="mailto:a1qualitydocuments@gmail.com">a1qualitydocuments@gmail.com</a></li>
          </ul>
        </div>
        <div class="footer-menu">
          <h3>Quick Links</h3>
          <ul class="footer-nav">
            <li><a href={base}>Home</a></li>
            <li><a href={`${base}estate-planning/`}>Estate Planning</a></li>
            <li><a href={`${base}contact-us/`}>Contact &amp; Book</a></li>
          </ul>
        </div>
      </div>
      <div class="site-footer__legal">
        <p class="footer-disclaimer">
          *The definition excerpts provided above come directly from the Oregon State Bar web site. It
          is important to realize that changes may occur in this area of law. This information is not
          intended to be legal advice regarding your particular needs or problem, and is not intended
          to replace the work of an attorney. We are not attorneys and we do not offer nor do we
          provide legal advice. Should you require legal advice, you should contact an attorney or the
          Oregon State Bar Association's Lawyer Referral Service at (503) 684-3763.
        </p>
        <p>&copy; {new Date().getFullYear()} A1 Quality Paralegal. All rights reserved.</p>
      </div>
    </footer>

    <script>
      // Mobile nav toggle
      const toggle = document.querySelector<HTMLButtonElement>('.nav-toggle');
      const nav = document.getElementById('primary-nav');
      if (toggle && nav) {
        toggle.addEventListener('click', () => {
          const open = toggle.getAttribute('aria-expanded') === 'true';
          toggle.setAttribute('aria-expanded', String(!open));
          nav.classList.toggle('is-open');
        });
        nav.querySelectorAll('a').forEach(a => {
          a.addEventListener('click', () => {
            toggle.setAttribute('aria-expanded', 'false');
            nav.classList.remove('is-open');
          });
        });
      }

      // More Services dropdown
      const dropdownToggle = document.querySelector<HTMLButtonElement>('.nav-dropdown-toggle');
      const dropdown = document.querySelector<HTMLElement>('.nav-dropdown');
      if (dropdownToggle && dropdown) {
        dropdownToggle.addEventListener('click', (e) => {
          e.stopPropagation();
          const open = dropdownToggle.getAttribute('aria-expanded') === 'true';
          dropdownToggle.setAttribute('aria-expanded', String(!open));
          dropdown.classList.toggle('is-open');
        });
        document.addEventListener('click', () => {
          dropdownToggle.setAttribute('aria-expanded', 'false');
          dropdown.classList.remove('is-open');
        });
        dropdown.querySelectorAll('a').forEach(a => {
          a.addEventListener('click', () => {
            dropdownToggle.setAttribute('aria-expanded', 'false');
            dropdown.classList.remove('is-open');
          });
        });
      }

      // Scroll reveals via IntersectionObserver
      const reveals = document.querySelectorAll<HTMLElement>('.reveal');
      if ('IntersectionObserver' in window) {
        const io = new IntersectionObserver(
          entries => {
            entries.forEach(e => {
              if (e.isIntersecting) {
                e.target.classList.add('is-visible');
                io.unobserve(e.target);
              }
            });
          },
          { threshold: 0.08 }
        );
        reveals.forEach(el => io.observe(el));
      } else {
        reveals.forEach(el => el.classList.add('is-visible'));
      }
    </script>
  </body>
</html>
